# Copy this file to local.properties and fill in your keys.
# local.properties is git-ignored — never commit API keys.

sdk.dir=/path/to/android/sdk

# Gemini API key — required for prompt translation via Google Gemini.
# Obtain from https://aistudio.google.com/app/apikey
gemini.api.key=YOUR_GEMINI_API_KEY

# Self-hosted server URLs (MusicGen remains on the local server for audio generation)
songgen.base.url=http://82.10.52.198:8000

# Legacy Gemma URL — no longer used; Gemini replaces Gemma for prompt translation.
# gemma.base.url=http://82.10.52.198:8001
